import 'package:flutter/material.dart';
class ParentAnalyticsScreen extends StatelessWidget {
  const ParentAnalyticsScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('ParentAnalyticsScreen')));
}
