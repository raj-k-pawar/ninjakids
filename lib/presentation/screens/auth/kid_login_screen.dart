import 'package:flutter/material.dart';
class KidLoginScreen extends StatelessWidget {
  const KidLoginScreen({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('KidLoginScreen')));
}
